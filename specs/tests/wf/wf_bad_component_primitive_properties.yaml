openapi: 3.1.0
paths:
  /ping:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: string
components:
  schemas:
    BadPrimitive:
      type: string
      properties:
        bogus:
          type: string