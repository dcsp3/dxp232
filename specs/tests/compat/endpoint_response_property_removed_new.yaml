openapi: 3.1.0
paths:
  /lookup:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                properties:
                  id:
                    type: integer
                required:
                  - id
components:
  schemas: {}
